# Нормалізація закінчень рядків: у репозиторії завжди LF.
# Текстові файли git визначає автоматично; на диску теж LF (eol=lf).
* text=auto eol=lf

# Бінарні файли не чіпати (щоб не псувати EOL-конвертацією).
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
