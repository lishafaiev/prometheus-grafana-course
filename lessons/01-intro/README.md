# Заняття 1 — Вступ до моніторингу та основні концепції

Дедлайн ДЗ: 23 червня, 23:59 · Макс. балів: 20

## Конспект
<!-- Основні концепції: метрики, scrape, target, exporter, time series... -->
-

## Домашнє завдання

**Завдання:**
1. Встановити Prometheus і Node Exporter на основну ОС.
2. Зібрати метрики своєї системи.
3. Проаналізувати prometheus.yml-файл.

**Що здаємо:**
- [ ] Файл із командами встановлення Prometheus та Node Exporter → `install.sh`
- [ ] Список метрик, що показують використання CPU → див. нижче
- [ ] Файл `prometheus.yml`
- [ ] Який інтервал збору метрик налаштовано? → **відповідь:**
- [ ] Скільки джерел метрик (targets) налаштовано? → **відповідь:**

### Метрики використання CPU
<!-- напр. node_cpu_seconds_total, rate(node_cpu_seconds_total[5m]) ... -->
-

### Аналіз prometheus.yml
- `scrape_interval`:
- `scrape_configs` / job-и:
- targets:
