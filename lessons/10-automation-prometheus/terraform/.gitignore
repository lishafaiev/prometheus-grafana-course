# Локальний кеш провайдерів і модулів
.terraform/

# State містить повний стан інфраструктури (включно з sensitive-значеннями) —
# у публічний репозиторій не потрапляє
*.tfstate
*.tfstate.*

# Локальні перевизначення змінних
*.auto.tfvars
terraform.tfvars

crash.log
